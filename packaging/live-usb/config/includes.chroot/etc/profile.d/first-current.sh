# First Current Quant OS: the user's home lives on the encrypted persistence
# partition when one is present, so configuration, keys and data survive reboots.
export QUANTOS_HOME="$HOME/FirstCurrent"
export QUANTOS_VENDOR_DIR=/opt/quantos/vendor
