#!/bin/sh
# Enable the terminal service and the daily timer for every new user.
set -eu
install -d /etc/skel/.config/systemd/user/default.target.wants /etc/skel/.config/systemd/user/timers.target.wants
ln -sf ../quantos-terminal.service /etc/skel/.config/systemd/user/default.target.wants/quantos-terminal.service
ln -sf ../quantos-daily.timer /etc/skel/.config/systemd/user/timers.target.wants/quantos-daily.timer
chmod +x /etc/skel/Desktop/*.desktop
